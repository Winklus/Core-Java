public class ExcepHirachy {
  // * OBJECT -> THROWABLE -> throwable is divide into two (1) ERRORS  (2) EXCEPTIONS

  // * For errors when they happens their is nothing u can do example of errors includes (1) out of memory error (2) IO errors (3) Thread death (4) virtual Machine error.


  // * Example of EXCEPTIONS include the following (1) Runtime exception (2) IO exception (3) SQL exception...

  // * All Runtime exceptions are called Unchecked exception while IO & SQL exception are called checked exception cos the compiler compel u to handle them
}
